# 64M 完整预训练 —— 跑到底，产出可用权重
#
# ⚠️ 用按需而非 Spot：2026-09-10 实测 Spot 跑到 14 分钟被抢占，
#    Pod 直接从 API 消失（404），容器盘连同全部进度丢失。
#    一次性训练省下的 Spot 折扣（约 $0.5）远低于归零的损失。
#    需要 Spot 的前提是有 network volume 能接续 —— 而挂卷的机器当前开不出来。
#
# 与 smoke-test.env 的区别：这是真的要把模型训出来，不是验证链路。
#
# ⚠️ 不挂 network volume 时的关键操作：
#    训练完成后必须在终止 Pod 之前把权重下载下来，否则随容器盘一起消失。
#    已实证：Pod stop/start 后 /workspace 全空。
#
# 规模：pretrain_t2t_mini.jsonl（1.24GB，39,695 iters/epoch @ 单卡）
#       2 epochs，2×4090 DDP ≈ 1 小时
# 成本：2×4090 Spot $0.68/hr × ~1h ≈ **$0.70**

IMAGE_NAME=runpod/pytorch:1.1.0-cu1281-torch260-ubuntu2204
BOOTSTRAP_URL=https://raw.githubusercontent.com/AiMeshes/minimind-runpod/d1473b629f1d8c567aec34e6e11a894e99f036bb/entrypoint.sh

# --- 2 卡：8/4 卡当前无货，2 卡墙钟减半总价相同 ---
GPU_TYPE=NVIDIA GeForce RTX 4090
GPU_COUNT=2
CONTAINER_DISK_GB=60
PORTS=8888/http,22/tcp

CLOUD_TYPE=COMMUNITY

# ⚠️ 必须约束 CUDA 版本：镜像声明 cuda>=12.8，部分老机器驱动不满足。
#    不加这条的表现：Pod 显示 RUNNING 但容器起不来，无限重试，
#    而且 REST 没有日志端点 —— 只有控制台能看到真实报错。
ALLOWED_CUDA_VERSIONS=12.8
INTERRUPTIBLE=0

# 不挂卷（挂卷的机器当前开不出来）。权重靠训练后下载取回。

DATA_URL=https://huggingface.co/datasets/jingyaogong/minimind_dataset/resolve/main/pretrain_t2t_mini.jsonl
MINIMIND_REPO=https://github.com/AiMeshes/minimind.git

PIP_PACKAGES=transformers==4.57.6 datasets==3.6.0
INSTALL_DEPS=1

START_SSHD=1
PUBLIC_IP=1

# --- 训练超参：minimind 默认值，跑满 2 epochs ---
# save_interval 1000：2 卡每 epoch 约 19,847 iters，每个 epoch 存约 20 次
TRAIN_ARGS=--epochs 2 --batch_size 32 --learning_rate 5e-4 --save_interval 1000 --log_interval 100

POD_NAME=minimind-64m-full
